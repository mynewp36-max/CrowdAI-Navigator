server {
    listen       8080;
    server_name  localhost;

    error_page 404 /index.html;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html;
    }
}
